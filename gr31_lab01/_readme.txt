ex2.1 contains the vhdl files for the behavioural architecture of the generic mux21.
ex2.2 contains the vhdl files for the structural architecture of the generic mux21.

ex3.1 contains the netlist for the async. register
ex3.2 contains the netlist for the sync. register

ex4.1.1 contains the post syntesys netlist for the behavioral mux
ex4.1.2 constains the post syntesys netlist for the strctural mux

ex4.2.1 contains the posty syntesys netlist for the async. register
ex4.2.2 contains the post sysesys netlist for the sync. register

ex6.1 also contains the files for the simulation of ex6.2
For the implementation of the accumulator we use a sync. register. All the implementations 
of the components use in this exercise, except for the register, are behavioural.

