ex2.1.4, the P4 adder also handles subtraction. The ex2.1.4 folder also contains a testbench that only simulates a critical case and the pdf of the report of the associated waveform.

ex2.2.1, the booth multiplier is for signed Numbers, so it's normal that, for example 0001*1011=11111011, because the algorithm works in 2's complement!.
The ex2.2.1 folder also contains a testbench that only simulates a critical case and the pdf of the report of the associated waveform.